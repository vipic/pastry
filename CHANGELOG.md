# Changelog

面向用户的版本说明。GitHub Release 的 commit 列表仍由 `release.sh` 自动生成；本文件用白话记录「对日常使用有影响」的变化。

发布新版本时：把 `Unreleased` 里的条目挪到对应版本节，并按需补充用户可见说明。

## Unreleased

## 1.18.0 — 2026-09-21

- 在托盘顶部按住鼠标时，整个托盘会立即缩小反馈，无需先移动指针确认拖拽是否生效；松手后恢复。

## 1.17.2 — 2026-09-21

- 打开搜索栏不再把卡片列表滚回开头，侧边紧凑托盘展开搜索时也会保持原有宽度。

## 1.17.1 — 2026-09-21

- “关于”页面现在直接显示当前版本和构建号，无需再前往“更新”页面查找。

## 1.17.0 — 2026-09-21

- 开发诊断记录新增起始时间、每日功能计数和正常使用、开发版、正式烟测三类上下文，语义索引失败也会留下可排查的非敏感错误分类。
- 数据库启动改为按实际表结构补齐缺失字段，不再依赖可能与真实结构不一致的版本字段，部分完成的旧结构可以安全恢复。

## 1.16.2 — 2026-09-21

- 首次引导的复制步骤明确说明不会自动修改剪贴板，并将“暂不复制”“后退”“继续”统一放到底部导航，用户无需复制示例也能继续设置。

## 1.16.1 — 2026-09-21

- 紧凑模式所有条目的悬停操作统一使用自适应 Liquid Glass 底板，在明暗内容上自动保持对比度和一致位置；常规卡片样式不变。
- 设置中的托盘打开位置恢复为屏幕左侧、右侧和底侧三个明确选项，并移除实验功能各区块重复的“实验性”标记。
- 搜索状态下按 Esc 只关闭搜索并清空关键字，不再清除来源、类型、时间、备注等结构化筛选。
- 托盘每次打开都会从卡片列表起点开始；拖拽停靠只在当前显示会话生效，重新打开后恢复设置中选择的固定位置。

## 1.16.0 — 2026-09-20

- 实验功能新增侧边托盘紧凑列表开关；链接显示网页标题和地址，图片与单文件统一为左侧信息和右侧预览，多文件纵向显示文件图标和名称，纯文本与富文本保持全宽文字布局；紧凑托盘进一步收窄并统一顶部控件与外框圆角间距，悬停操作固定在卡片右下角。
- 图片和文件预览上的悬停操作增加材质底板与描边，在明暗背景上都能保持清晰。
- Command 数字快捷选择改为按当前滚动视口编号，滚动后的可见卡片也可直接使用 `⌘1`–`⌘9`。
- 修复按住 Command 切换到其他应用后再松开时，托盘数字角标可能一直显示的问题。
- 修复屏幕底部被程序坞占用时，左右侧托盘无法从顶部拖回底部的问题。

## 1.15.3 — 2026-09-20

- 智能找回移至“实验功能”，先显示 Apple 设备端模型状态；仅在模型可用时允许开启。
- 修复筛选面板内滚动来源应用时，托盘卡片会同时横向滚动的问题。

## 1.15.2 — 2026-09-20

- 文件在同一磁盘内移动或重命名后，历史卡片仍可通过系统文件书签打开、预览、拖拽并在 Finder 中定位。

## 1.15.1 — 2026-09-20

- 移除托盘外侧阴影，使托盘停靠在左右边缘时保持干净自然。
- 仅在智能找回已开启且 Apple Intelligence 可用时建立语义索引；模型在回填中变为不可用时会停止处理。

## 1.15.0 — 2026-09-20

- 托盘支持从顶部空白区域拖动到左侧、右侧或底部；进入目标区域时会显示虚线预览，松手后完成停靠。
- 新增托盘固定按钮，固定后可在其他应用中继续工作，并让托盘保持可用。
- 托盘打开位置新增“固定在底部”和“跟随记忆”模式；收藏相关图标统一改为书签。

## 1.14.1 — 2026-09-20

- 修复未挂载外置卷中的普通文件记录不会按每日失效清理规则删除的问题。

## 1.14.0 — 2026-09-20

- 每天自动清理全部文件路径都已失效的普通文件和图片记录；收藏、多文件中仍有可用路径的记录，以及未挂载外置卷中的记录会继续保留。

## 1.13.12 — 2026-09-19

- 精简设备端模型和智能找回相关文案，只说明 Pastry 使用的 macOS、Apple Intelligence 与本地处理能力。

## 1.13.11 — 2026-09-19

- 修正语义索引操作入口：只有索引明确失败时才显示“继续建立”，正常建立中仍保持“重新建立”。

## 1.13.10 — 2026-09-19

- 修复设备端语义索引中断后只能重新开始的问题；设置页现在可以从已完成的位置继续建立。
- 只有主动选择重建时才会清除已有语义索引进度。

## 1.13.9 — 2026-09-19

- 新增实验性的设备端智能找回：可用自然语言描述时间、来源、类型或概念线索，并结合本地语义索引找回相关历史。
- 智能找回默认关闭；开启后使用 Apple 设备端模型在本机分析历史内容。
- 通用设置新增模型可用状态、语义索引进度和安全的重建入口；关闭功能后仍保持原有关键词搜索体验。
- 修复连续中文和多关键词搜索可能漏掉历史内容的问题，并避免模型猜测未明确指定的来源应用或内容类型。

## 1.13.5 — 2026-09-08

- 优化托盘滚轮手感：侧滚轮保持细微移动并限制快速连滚幅度，普通滚轮可横向快速浏览且保留设备加速。

## 1.13.4 — 2026-09-08

- 优化托盘侧轮手感：慢滚可细微移动，快速滚动会跟随鼠标驱动的加速幅度。

## 1.13.3 — 2026-09-08

- 修复托盘内鼠标侧滚轮无法横向浏览卡片的问题；滚动改用稳定的 SwiftUI 卡片定位，不再依赖私有视图层级。

## 1.13.0 — 2026-08-13

- 新增发布工作流契约测试，持续锁定统一验证、显式版本、原子发布和 CI 制品边界。
- 发布流程改为强制统一验证，新增正式 DMG 首次启动验收与 SHA-256 附件。
- 正式发布改用原子推送并拒绝覆盖已有 Release；CI 只验证 release 构建，不再生成无稳定签名来源的 DMG。

## 1.12.2 — 2026-07-30

- 减少快捷键重注册与粘贴路径上的卡顿，唤起和粘贴更跟手。

## 1.12.1 — 2026-07-26

- 修复深色模式下设置页下拉框看不清的问题。
- 去掉全局滚轮监听，避免被系统提示「输入监控」权限；面板内滚轮仍正常。

## 1.12.0 — 2026-07-26

- 搜索支持按备注筛选；可用显式搜索快捷键打开搜索栏。
- 卡片选中状态更清晰。
- About 作者信息更新为 Vipic。

## 1.11.0 — 2026-07-15

- 首次引导的图标切换改用轨迹绘制动画，步骤反馈更顺滑。

## 1.10.0 — 2026-07-15

- 收敛界面设计 token，视觉档位更一致。
- 完善本地开发诊断日志（仅本机，不上报）。

## 1.9.1 — 2026-07-14

- 修复完成首次引导后面板偶发自动关闭。

## 1.9.0 — 2026-07-14

- 新增首次启动引导：授权、快捷键、复制首条记录等分步说明。

## 1.8.7 — 2026-07-13

- 横向卡带侧向滚动改为像素跟手，滚动手感更稳。

## 1.8.6 — 2026-07-12

- 设置页开关说明文案与更新状态图标微调。

## 1.8.4 — 2026-07-02

- 剪贴板监听改为定时轮询，不再使用全局键盘事件监听。

## 1.8.3 — 2026-07-02

- 主线程卡住时自动恢复事件系统。
- 数据库密钥改为本地密钥文件管理，不再依赖 Keychain。

## 1.8.1 — 2026-07-02

- 加固数据库加密与迁移完整性，以及远程预览 / 更新校验相关安全边界。

## 1.8.0 — 2026-06-29

- 任意历史卡片都可添加备注（不再仅限收藏项）。

## 1.7.1 — 2026-06-21

- 菜单栏移除「清空普通历史」，降低误操作风险。

## 1.7.0 — 2026-06-21

- 文件卡片在文件名后显示文件大小。

## 1.6.0 — 2026-06-20

- 收藏项支持备注。

## 1.5.0 — 2026-06-19

- 更新页可展示最近版本说明，并在进入时自动检查更新。
- 精简菜单栏右键入口。
